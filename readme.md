### tokio-watch

A small WIP/PoC wrapper to integrate notify.rs into the tokio eco-system. works similar to `tail -f` on linux. Intended to be a cross platform alternative to piping the outpput of `tail -f` into programs.